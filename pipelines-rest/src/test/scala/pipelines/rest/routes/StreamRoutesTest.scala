package pipelines.rest.routes

class StreamRoutesTest extends BaseRoutesTest {

  "StreamRoutes backlog" ignore {
    "websocket sources should write down" in {

      val streamRoutes = new StreamRoutes()

    }
  }
}
